{
	"name": "@acme/ui",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"types": "src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./package.json": "./package.json"
	},
	"scripts": {
		"build": "echo \"@acme/ui does not have a build because it is built via the app using it\"",
		"lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
		"clean": "rimraf dist node_modules/.vite"
	},
	"peerDependencies": {
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"@types/react": "^18.2.15",
		"@types/react-dom": "^18.2.7"
	}
}
